{% extends 'backoffice/backofficebase.html.twig' %}

{% block title %}Back-MovieFlix{% endblock %}

{% block body %}
    <h1>Back-MovieFlix</h1>
    <div class="d-flex">
        <div class="card" style="width: 18rem;margin:.75em">
            <div class="card-header">Movies</div>
            <div class="card-body">
                <a href="{{ path('backoffice_movie_new') }}" class="btn btn-success card-link">Create new</a>
                <a href="{{ path('backoffice_movie_index') }}" class="btn btn-info card-link">View All</a>
            </div>
        </div>
        <div class="card" style="width: 18rem;margin:.75em">
            <div class="card-header">Genres</div>
            <div class="card-body">
                <a href="{{ path('backoffice_genre_new') }}" class="btn btn-success card-link">Create new</a>
                <a href="{{ path('backoffice_genre_index') }}" class="btn btn-info card-link">View All</a>
            </div>
        </div>
        <div class="card" style="width: 18rem;margin:.75em">
            <div class="card-header">Person</div>
            <div class="card-body">
                <a href="{{ path('backoffice_person_new') }}" class="btn btn-success card-link">Create new</a>
                <a href="{{ path('backoffice_person_index') }}" class="btn btn-info card-link">View All</a>
            </div>
        </div>
        <div class="card" style="width: 18rem;margin:.75em">
            <div class="card-header">Casting</div>
            <div class="card-body">
                <a href="{{ path('backoffice_casting_new') }}" class="btn btn-success card-link">Create new</a>
                <a href="{{ path('backoffice_casting_index') }}" class="btn btn-info card-link">View All</a>
            </div>
        </div>
        <div class="card" style="width: 18rem;margin:.75em">
            <div class="card-header">Season</div>
            <div class="card-body">
                <a href="{{ path('backoffice_season_new') }}" class="btn btn-success card-link">Create new</a>
                <a href="{{ path('backoffice_season_index') }}" class="btn btn-info card-link">View All</a>
            </div>
        </div>
    </div>
{% endblock %}
