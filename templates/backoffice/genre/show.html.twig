{% extends 'backoffice/backofficebase.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backoffice_genre_index') }}" class="btn btn-info">back to list</a>

    <a href="{{ path('backoffice_genre_edit', {'id': genre.id}) }}" class="btn btn-warning">edit</a>

    {{ include('backoffice/genre/_delete_form.html.twig') }}
{% endblock %}
